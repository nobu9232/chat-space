.message_list
  .upper--info
    %p.upper--info__user 
      = message.user.name
    %p.upper--info__date 
      = message.created_at.strftime("%Y/%m/%d %H:%M")
  - if message.text.present?
    = message.text
    = image_tag message.image.url, class: 'message--text ' if message.image.present?